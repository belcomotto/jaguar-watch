node_modules
dist
.DS_Store
.env
.env.local

# Sentinel imagery — large binary files, regenerated by download script
public/sentinel/

# Raw GeoTIFF source files
public/data/*.tif

# Raw Sentinel pipeline data — not needed by the app, regenerated by batch scripts
jaguar-watch-data/
